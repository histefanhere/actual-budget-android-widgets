Build and install the debug APK to the connected device by running:

```
powershell.exe -Command "& '.\gradlew.bat' installDebug"
```

Run this from the project root directory `C:\Users\stefa\Desktop\projects\ActualBudgetAndroidWidgets`.
